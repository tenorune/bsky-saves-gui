// Build timestamp injected by Vite's `define` config in vite.config.ts.
// Visible in the app footer to make it obvious which build is loaded — useful
// when verifying that a hard reload actually picked up new code.
export const BUILD_TIME: string = __BUILD_TIME__;
